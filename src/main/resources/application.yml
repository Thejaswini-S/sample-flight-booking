server:
  port: 8081

spring:
  application:
    name: flight-booking-api
  profiles:
    # Default profile for local runs; override via SPRING_PROFILES_ACTIVE or --spring.profiles.active
    active: dev

# OpenAPI / Swagger UI (springdoc)
springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    operations-sorter: method

# Directory used by logback-spring.xml for centralized rolling logs
logging:
  file:
    path: logs

# Application configuration bound via @ConfigurationProperties (see FlightSeedProperties).
# Seed data is environment-specific and overridden per profile (dev enables sample flights).
flight-booking:
  seed:
    enabled: false
    flights: []
